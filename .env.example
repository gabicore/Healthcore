# Hostinger MySQL
# Hostname: hPanel → Databases → Remote MySQL (topo da página)
# No servidor Hostinger (app Node no mesmo host), use localhost no lugar do hostname remoto.
DATABASE_URL="mysql://u186419476_admin:SENHA@COLE_HOSTNAME_REMOTE:3306/u186419476_healthcore"
